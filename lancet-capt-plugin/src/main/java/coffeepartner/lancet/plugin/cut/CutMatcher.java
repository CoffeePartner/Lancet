package coffeepartner.lancet.plugin.cut;

import coffeepartner.capt.plugin.api.asm.CaptClassVisitor;
import org.objectweb.asm.tree.MethodInsnNode;

public class CutMatcher extends CaptClassVisitor {

    public boolean match(MethodInsnNode )

    public enum Type {
        CLASS, METHOD, TRY_CATCH, INVOKE_METHOD, INVOKE_FIELD,
    }
}
